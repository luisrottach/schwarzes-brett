
<!doctype html>
<html lang="de">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title or "Schwarzes Brett" }}</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
<script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
<script>
  if (window.netlifyIdentity) {
    window.netlifyIdentity.on("init", (user) => {
      const h = window.location.hash || "";
      // Invite-Link erkannt? Dann Signup-Dialog öffnen
      if (!user && (h.includes("invite") || h.includes("invite_token"))) {
        window.netlifyIdentity.open("signup");
      }
    });
  }
</script>
  <body>
    <header class="container">
      <h1><a href="/board/">Schwarzes Brett</a></h1>
    </header>
    <main class="container">
      {{ content | safe }}
    </main>
    <footer class="container small">
      <p>Powered by Netlify + Decap CMS + Eleventy</p>
    </footer>
  </body>
</html>
